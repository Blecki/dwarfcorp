gob-nosword

frame size:32x40
animations: walk, jump, attack (empty-handed)


gob-swordsonly

frame size: 64x40
animations: walk, jump, attack


gob-withsword

frame size: 64x40
animations: walk, jump, attack



ideally, overlay frames of gob-swordsonly on top of frames of gob-nosword (with appropriate 16px offset)
for 64x40 frames, simulating the appearance of each frame of gob-withsword.

if figuring this out isn't at the top of your list of priorities, well, that's what gob-withsword is there for.